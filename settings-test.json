{
    "galaxy.meteor.com": {
        "env": {
            "ROOT_URL": "https://reentry.me",
            "MONGO_URL": "mongodb://username:password@server:port/database"
        }
    },
    "orion-cli": {
        "profile": "default",
        "config": "private/orion-config.json"
    }
}
